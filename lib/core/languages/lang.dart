import 'package:flutter/material.dart';

const Locale arabicLocale = Locale('ar');
const String localePath = 'assets/translations';
// const Locale englishLocale =  Locale('en')  ;
List<Locale> supportedLocales = const [arabicLocale];

// flutter pub run easy_localization:generate -S assets/translations -O lib/core/languages -f keys -o locale_keys.g.dart
